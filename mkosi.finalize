#!/usr/bin/env bash
set -Eeuo pipefail

[ "$1" == "final" ] || exit 0

exit 0

cd $OUTPUTDIR

mount \
 -o loop \
 image.root.raw \
 image.root.dir

mksquashfs -comp zstd image.root.dir image.squashfs


