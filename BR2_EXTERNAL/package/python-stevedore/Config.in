# vim: noexpandtab filetype=make
config BR2_PACKAGE_PYTHON_STEVEDORE
	bool "python-stevedore"
	depends on BR2_PACKAGE_PYTHON3
	select BR2_PACKAGE_PYTHON_PBR
	select BR2_PACKAGE_PYTHON_SIX
	help
		stevedore avoids creating yet another extension mechanism by building on top of `setuptools entry points`_. The code for managing entry points tends to be repetitive, though, so stevedore provides manager classes for implementing common patterns for using dynamically loaded extensions
		
		https://docs.openstack.org/stevedore/latest/
